
# Environment & Server

NODE_ENV=development
PORT=3000
IP=10.10.7.44

# App name
APP_NAME=Easy Express

# Database (MongoDB)
MONGODB_URI=mongodb://127.0.0.1:27017/elysia

# Individual parts (if you prefer to build URI in code)
DB_HOST=127.0.0.1
DB_PORT=27017
DB_NAME=elysia

# JWT & Security

JWT_SECRET=your-very-long-random-secret-key-change-this-2026
JWT_EXPIRES_IN=7d
JWT_BYCRYPT_SALT=14            

# Email / SMTP (Gmail / SendGrid / etc.)
MAIL_HOST=smtp.gmail.com
MAIL_PORT=587
MAIL_USER=example@gmail.com
MAIL_PASSWORD=your-email-app-password
EMAIL_FROM=example@gmail.com

# Redis (if you use caching/session/queue)
REDIS_HOST=127.0.0.1
REDIS_PORT=6379
REDIS_PASSWORD=your-redis-password
REDIS_URI=redis://:your-redis-password@127.0.0.1:6379

# CORS (frontend origins)
ALLOWED_ORIGINS=http://10.10.7.44:4173,http://10.10.7.44:5000,http://localhost:5173,http://localhost:3000

# Logging & Debugging
LOG_LEVEL=info         
